SELECT orderdetails.productCode, products.productName, SUM(orderdetails.quantityOrdered) as totalQuantitySold FROM orderdetails JOIN products ON orderdetails.productCode = products.productCode GROUP BY orderdetails.productCode, products.productName;